# Energy Retention Command Centre dashboard

Install Node dependencies, then launch the dashboard:

```powershell
npm install
npm run dev
```

The dashboard expects the Flask API at `http://127.0.0.1:5000`. Override it with
`VITE_API_BASE_URL` when needed.
